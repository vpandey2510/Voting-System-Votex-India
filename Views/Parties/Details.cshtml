@model VotingSystem.Models.Party

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Party</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.FlagImagePath)
        </dt>
        <dd class="col-sm-10">
            <img src="@Model.FlagImagePath" alt="Flag Image">
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.PartyID">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
